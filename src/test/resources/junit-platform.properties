# Auto-register ServiceLoader-provided JUnit Jupiter extensions (see
# META-INF/services/org.junit.jupiter.api.extension.Extension). Used to install
# a disk-loaded TileRegistry before any test runs — see TileRegistryTestInstaller.
junit.jupiter.extensions.autodetection.enabled=true
